server:
  port: 8081

spring:
  application:
    name: jwks-dev

management:
  server:
    port: 15020
  endpoints:
    web:
      exposure:
        include: health # "*"
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
